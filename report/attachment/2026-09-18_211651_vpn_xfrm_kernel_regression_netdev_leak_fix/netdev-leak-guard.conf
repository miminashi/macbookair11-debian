# /etc/netdev-leak-guard.conf — 48-netdev-leak-guard の設定 (sh 形式)
# ACTION: リーク検知時の動作 (poweroff | reboot)。試験時のみ reboot にする
ACTION=poweroff
# GRACE: 電源断までの猶予 (秒)
GRACE=60
# DRYRUN: 1 にすると判定・ログ・wall のみでスケジュールしない
DRYRUN=
